# Important links

I don’t have access to external webpages from that link. Do you want me to:

* Import and convert the exact Markdown from that article into GitBook-optimized Markdown? If so, please paste the article’s Markdown (or the raw text) here. I’ll convert it using GitBook blocks and preserve all links and structure; or
* Summarize what happens when you run any Kubernetes creation command (like kubectl run / kubectl create / kubectl apply) based on general Kubernetes behavior? I can give a concise, accurate explanation of the client and server-side flow (kubectl, API server, authn/authz, admission controllers, etc.).

Which would you prefer?
